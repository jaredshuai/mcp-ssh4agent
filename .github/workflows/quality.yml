name: Code Quality

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [24.x]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    
    - name: Cache Node modules
      uses: actions/cache@v3
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    
    - name: Install Node dependencies
      run: |
        npm install
        npm install --save-dev eslint prettier
    
    - name: Run JavaScript syntax check
      run: |
        node --check src/index.js
        node --check src/ssh-manager.js
    
    - name: Run ESLint
      run: npx eslint src/
      continue-on-error: true

    # Type-checks the JSDoc annotations over plain JS (no build, nothing emitted).
    # Not continue-on-error: this gate is only useful if it can fail the build.
    - name: Typecheck (JSDoc via tsc)
      run: npm run typecheck

    - name: Run knip (dead code & unused dependencies)
      run: npx knip

    - name: Run validation script
      run: npm run validate
    
    - name: Check for secrets
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        base: ${{ github.event.repository.default_branch }}
        head: HEAD
      continue-on-error: true